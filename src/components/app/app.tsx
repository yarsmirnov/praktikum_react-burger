import React from 'react';
import AppHeader from '../app-header/app-header';

const App = () => {
  return (
    <React.Fragment>
      <AppHeader />
    </React.Fragment>
  );
};

export default App;
